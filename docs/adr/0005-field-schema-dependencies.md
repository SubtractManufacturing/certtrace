# Field schema supports dependencies from day one

Even though shape-driven size templates are deferred, the field definition model includes dependency/visibility rules up front (e.g. Alloy options filtered by Family; later, dimension fields shown by Shape). Rejected: shipping a flat schema and retrofitting dependencies when sizes land — that would force a schema redesign after libraries exist. Size-by-shape templates remain a later layer on this foundation.
