# Size unit is an app preset, not a field

Inch and millimetre are fixed in the app so a shop can enter and display Size in either system. Unit is a UI helper and a property of the Size, not a library Field — shops cannot add, rename, or delete units in the field schema. Rejected: a `size_unit` field, which would mix a closed app choice into the customizable field bag and let Unit drift from Size.
